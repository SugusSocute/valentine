<!DOCTYPE html>
<html lang="th">
<head>
<meta charset="UTF-8" />
<meta name="viewport" content="width=device-width, initial-scale=1.0"/>
<title>💖 Valentine</title>

<style>
body {
  margin: 0;
  height: 100vh;
  overflow: hidden;
  background: linear-gradient(135deg, #ffb6c1, #ffe4ec);
  display: flex;
  justify-content: center;
  align-items: center;
  font-family: "Kanit", sans-serif;
}

.card {
  background: #fff;
  padding: 40px 30px;
  border-radius: 25px;
  text-align: center;
  width: 90%;
  max-width: 360px;
  box-shadow: 0 15px 35px rgba(0,0,0,0.15);
  z-index: 10;
  animation: pop .5s ease;
}

@keyframes pop {
  from { transform: scale(.85); opacity: 0; }
  to { transform: scale(1); opacity: 1; }
}

h1 {
  color: #ff4f87;
  font-size: 26px;
  margin-bottom: 25px;
}

button {
  background: #ff4f87;
  color: #fff;
  border: none;
  border-radius: 30px;
  padding: 12px 26px;
  font-size: 18px;
  cursor: pointer;
  margin: 8px;
  transition: .3s;
}

button:hover {
  transform: scale(1.08);
}

.no {
  background: #ccc;
  color: #555;
  position: absolute;
}

.heart {
  font-size: 42px;
  margin-bottom: 15px;
  animation: beat 1s infinite;
}

@keyframes beat {
  0% { transform: scale(1); }
  50% { transform: scale(1.2); }
  100% { transform: scale(1); }
}

.floating-heart {
  position: absolute;
  bottom: -40px;
  font-size: 24px;
  animation: floatUp 6s linear infinite;
  opacity: .8;
}

@keyframes floatUp {
  from { transform: translateY(0) scale(1); opacity: 1; }
  to { transform: translateY(-120vh) scale(1.6); opacity: 0; }
}
</style>
</head>

<body>

<audio id="bgMusic" loop>
  <source src="เพื่อนรัก.mp3" type="audio/mpeg">
</audio>

<div class="card" id="card">
  <div class="heart">💝</div>
  <h1>Happy Valentine Day!!</h1>
  <button onclick="next()">ถัดไป ➜</button>
</div>

<script>
let step = 0;
let musicStarted = false;

const texts = [
  "ผมมีอะไรจะบอกคุณ",
  "จะบอกละน้าา",
  "จะบอกละน้าา",
  "เป็นแฟนกันมั้ยย 💕"
];

function startMusic() {
  if (!musicStarted) {
    const music = document.getElementById("bgMusic");
    music.volume = 0.8;
    music.play();
    musicStarted = true;
  }
}

function next() {
  startMusic();
  const card = document.getElementById("card");

  if (step < texts.length - 1) {
    card.innerHTML = `
      <div class="heart">💖</div>
      <h1>${texts[step]}</h1>
      <button onclick="next()">ถัดไป ➜</button>
    `;
    step++;
  } else {
    card.innerHTML = `
      <div class="heart">💘</div>
      <h1>${texts[step]}</h1>
      <button onclick="yes()">ตกลง 💕</button>
      <button class="no" id="noBtn"
        onmouseover="escapeNo()"
        onclick="escapeNo()">ไม่ตกลง 🙈</button>
    `;
  }
}

function escapeNo() {
  const btn = document.getElementById("noBtn");
  const x = Math.random() * (window.innerWidth - 120);
  const y = Math.random() * (window.innerHeight - 120);
  btn.style.left = x + "px";
  btn.style.top = y + "px";
}

function yes() {
  document.getElementById("card").innerHTML = `
    <div class="heart">💞</div>
    <h1>เย้! เราเป็นแฟนกันแล้วนะ 🥰</h1>
    <p style="font-size:18px;color:#555;">
      ขอบคุณที่รักในสิ่งที่ผมเป็นนะครับบ 💖
    </p>
  `;
}

setInterval(() => {
  const heart = document.createElement("div");
  heart.className = "floating-heart";
  heart.innerText = "💖";
  heart.style.left = Math.random() * 100 + "vw";
  heart.style.animationDuration = (Math.random() * 3 + 4) + "s";
  document.body.appendChild(heart);
  setTimeout(() => heart.remove(), 7000);
}, 350);
</script>

</body>
</html>
